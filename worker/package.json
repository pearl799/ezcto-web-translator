{
  "name": "ezcto-api-gateway",
  "version": "1.0.0",
  "description": "EZCTO API Gateway — Cloudflare Worker for translation asset library and content negotiation",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run",
    "tail": "wrangler tail",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.65.0"
  }
}
